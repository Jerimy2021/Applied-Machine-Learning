"""
Esquema de columnas del proyecto.

Aquí se declara cada columna del diccionario de datos (ver
reports/diccionario_datos.md): su nombre estándar (snake_case, sin tildes,
sin ñ), su tipo esperado y, si aplica, sus categorías válidas.

Todavía no se define el esquema real: se completa en Fase 1 a medida que se
audita cada archivo de data/raw/ (ver reglas de CLAUDE.md - Estándar de
columnas).
"""

# from dataclasses import dataclass
# import pandas as pd

# TODO: definir esquema por columna (nombre, dtype, categorías válidas)
# a medida que se audita cada archivo fuente en notebooks/00_inventario_fuentes.ipynb.
